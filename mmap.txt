# mmap

# 应用场景
***
虽然最初的设计并不是完全为了共享内存这个目的。只是提供了一个不同于一般对普通文件的访问方式。
但是实际共享内存也是其主要的应用之一。
而且当对一个文件频繁访问，并且指针来回移动时，调用mmap比常规方法快很多。

# 原型
***
void *mmap(void *addr, size_t len, int prot, int flags, int fd, off_t offset);
