通常用du命令来查看磁盘空间;

#惯用法#
查询home目录下各目录大小：du -sh /home/*
-s: summary, 输出目录下的总大小，不会显示子目录大小

发现磁盘空间不足快速找出占用空间最大的文件
目前能发现的方式，只有通过从根目录去递归du;

如果将该目录下的文件按字节排序：du -s /home/* | sort -rn
如果将该录下的文件按M排序: du -sh /home/* | sort -rn