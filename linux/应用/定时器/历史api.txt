# 历史api

***
int getitimer(int which,struct itimerval *value); 
int setitimer(int which,struct itimerval *newval, struct itimerval *oldval);

***
有限制，一个进程同一时刻只能有一个timer， 
setitmer计时器时间到达时，只能使用信号方式通知使用timer的进程

***
精度是ms