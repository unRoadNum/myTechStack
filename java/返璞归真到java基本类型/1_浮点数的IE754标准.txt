#浮点数表示#

计算器怎么存储浮点数，这个问题一直是我的知识盲区。

float和double是java的浮点数基本类型，默认是double类型。

浮点数在计算机中存储是按照如下格式来完成的，即IEEE754：

数符	阶码（含有阶符，就是有可能正或负）	尾数（默认是1.xxx，1隐藏） 

但是浮点数有个弊端：
无论是float和double类型都不能表示精确的值，如货币

为什么呢？
#二进制无法精确表示所有实数#
