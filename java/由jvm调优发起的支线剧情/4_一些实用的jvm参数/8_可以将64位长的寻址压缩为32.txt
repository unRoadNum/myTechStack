在64位的虚拟机上，应用程序所占内存的大小要远远超过32位版本（约为1.5倍左右）。

这是因为64位系统拥有更宽的寻址空间，与32位系统相比，指针对象的长度进行了翻倍。
为了解决这个问题，64位的JVM虚拟机可以使用-XX:+UseCompressedOops参数打开指针压缩，
从一定程度上减少了内存的消耗。

启用-XX:+UseCompressedOops后，可以对以下指针进行压缩：
1. Class的属性指针（静态成员变量）
2. 对象的属性指针
3. 普通对象数组的每个元素指针

虽然压缩指针可以节省内存，但是压缩和解压指针也会对JVM造成一定的性能损失。