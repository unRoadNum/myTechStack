#通配符?和T#

在泛型中使用的通配符就是？，那么它和T有什么区别呢？
为什么不直接用T，而用了一个"？"？

#<? extends fruit>和<T extends fruit>的区别就是所有的T都代表同一种类型#，比如
```
public <T> List<T> fill(T...t);
```
但是通配符?就没有这种限制，Plate<?>单纯的就表示：盘子里放了一个东西，具体是什么，我不知道。


